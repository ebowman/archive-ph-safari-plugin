
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Xcode build output (derived data) from build.sh
app/build/

# Owner-local codesigning override, sourced by build.sh if present.
.signing.env
